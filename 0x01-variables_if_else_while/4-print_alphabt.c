#include <stdio.h>
/**
 *main - lowercase
 *Return: 0
 */

int main(void)
{
	 char letter;

	for (letter = 'a'; letter <= 'z'; letter++)
	if (letter == 'e' || letter == 'q')
		{

		}
	else
		{
		putchar(letter);
		}

		putchar('\n');

	return (0);
}
