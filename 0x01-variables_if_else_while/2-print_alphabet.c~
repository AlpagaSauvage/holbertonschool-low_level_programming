#include <stdio.h>
/**
 *main - lowercase
 *Return: 0
 */

int main(void)
{
	 char ch;
		printf("\nLowercase characters : \n");
		for (ch = 'a'; ch <= 'z'; ch++)
	{
	printf("%c ", ch);
	}

    return (0);
}
