#include "main.h"

/**
 *print_square - square
 *@size- value
 */

void print_square(int size)
{
	int i = size, j;

	if (i > 0)
	{
		while (i > 0)
		{
			j = size;
			while (j > 0)
			{
				_putchar('#');
				j--;
			}
			_putchar('\n');
			j++;
			i--;
		}
	}
	else
	{
		_putchar('\n');
	}
}
